.event-for-box
  = link_to h(event.name), event
  %br
  %span.small
    = event.date_and_time.in_time_zone("Pacific Time (US & Canada)").strftime('%a, %b %d, %Y')
    - if event.date_and_time > Date.today.to_time
      = soon_indicator(event.date_and_time)

