- content_for :meta_title do
  = "Past #{@cms_config['site_settings']['events_title']}"
= render :partial => "shared/slice", :locals => {:column_section_id => nil, :controller => "events", :action => "past"}, :collection => @main_column_sections