- content_for :meta_title do
  New category

%h1= yield(:meta_title).strip

- form_for [:admin, @event_category], :html => { :onsubmit => "form_submitted('')" } do |f|
  = render :partial => "form", :locals => { :f => f }
